services:
  portainer:
    image: portainer/portainer-ce:latest
    container_name: portainer
    ports:
      - "{{ service.port }}:{{ service.port }}"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - "{{ ansible_user_dir }}/appdata/portainer:/data"
    restart: unless-stopped